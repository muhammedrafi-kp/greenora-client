export interface IDistrict {
    _id: string;
    name: string;
}

export interface IServiceArea {
    _id: string;
    name: string;
}

export interface IAddress {
    _id: string;
    name: string;
    mobile: string;
    pinCode: string;
    locality: string;
    addressLine: string;
}